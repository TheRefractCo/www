---
interface Props {
	campaign: string;
	thirdHref?: string;
	thirdLabel?: string;
	loadScript?: boolean;
}

const {
	campaign,
	thirdHref = '/paper/',
	thirdLabel = 'Learn more',
	loadScript = true,
} = Astro.props;
const storeUrl = `https://apps.microsoft.com/detail/9pjkn3wdpm1b?cid=${encodeURIComponent(campaign)}`;
---

<div class="paper-actions">
	<ms-store-badge
		productid="9pjkn3wdpm1b"
		cid={campaign}
		productname="Paper — PDFs, Books & News"
		window-mode="direct"
		theme="auto"
		size="small"
		language="en-us"
		animation="on"
		data-analytics-event="paper_store_click"
		data-analytics-action="store_badge"
		data-analytics-location={campaign}
	>
	</ms-store-badge>
	<noscript>
		<a
			class="round-action round-action--store-fallback"
			href={storeUrl}
			data-analytics-event="paper_store_click"
			data-analytics-action="store_fallback"
			data-analytics-location={campaign}
		>
			Get Paper from Microsoft Store <span aria-hidden="true">↗</span>
		</a>
	</noscript>
	<a
		class="round-action round-action--quiet"
		href={storeUrl}
		data-analytics-event="paper_store_click"
		data-analytics-action="store_link"
		data-analytics-location={campaign}
	>
		View on the Store <span aria-hidden="true">↗</span>
	</a>
	<a
		class="round-action round-action--paper"
		href={thirdHref}
		data-analytics-event="paper_detail_click"
		data-analytics-action={thirdLabel}
		data-analytics-location={campaign}
	>
		{thirdLabel} <span aria-hidden="true">→</span>
	</a>
</div>

{
	loadScript && (
		<script
			type="module"
			src="https://get.microsoft.com/badge/ms-store-badge.bundled.js"
			is:inline
		></script>
	)
}
