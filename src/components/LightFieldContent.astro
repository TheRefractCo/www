---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'section'> {
	className?: string;
}

const { class: classAttribute, className, ...attributes } = Astro.props;
---

<section class:list={['lightfield-content', classAttribute, className]} {...attributes}>
	<slot />
</section>
